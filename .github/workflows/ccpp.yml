name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@master
    - name: install build essential
      run: apt-get update && apt-get install bison
    - name: build
      run: ./inception_build.sh linux
